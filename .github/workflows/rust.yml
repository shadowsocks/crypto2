name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-check:
    name: Build and Check
    strategy:
      matrix:
        platform:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    runs-on: ${{ matrix.platform }}
    steps:
    - uses: actions/checkout@v2
    - uses: actions-rs/toolchain@v1
      with:
        toolchain: nightly
        components: clippy
        override: true
    - name: Check
      run: cargo clippy -- -W clippy::all -Z macro-backtrace
    - name: Build
      run: cargo build --verbose
    - name: Test
      run: cargo test --verbose
    - name: Docs
      run: cargo doc --verbose
